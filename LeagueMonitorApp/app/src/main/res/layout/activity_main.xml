<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <EditText android:id="@+id/field_summoner_name"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"

        android:hint="@string/field_summoner_name"
        android:inputType="textVisiblePassword" />

    <Button android:id="@+id/button_lookup_summoner"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/field_summoner_name"

        android:text="@string/button_summoner_info"
        android:onClick="lookupSummonerActivity" />

    <Button android:id="@+id/button_ping_test"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button_lookup_summoner"

        android:text="@string/button_ping_test"
        android:onClick="pingTestActivity"/>

</RelativeLayout>
